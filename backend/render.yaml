services:
  - type: web
    name: sitewatch-api
    plan: free
    runtime: python
    rootDir: backend
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      # SQLite inside the instance; see backend/gunicorn.conf.py. Overrides the
      # DATABASE_URL the deleted free Postgres left behind.
      - key: DATABASE_ENGINE
        value: sqlite
      - key: REDIS_URL
        fromService:
          name: sitewatch-redis
          type: redis
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: PYTHON_VERSION
        value: "3.12.3"
      - key: CELERY_TASK_ALWAYS_EAGER
        value: "True"

  - type: redis
    name: sitewatch-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []
